export type Tproject = {
  category: { name: string };
  main_image: { url: string };
  creator: { name: string };
  end_time: number;
  total_pledged: number;
  funding_goal: number;
  blurb: string;
  id: number;
  name: string;
};
